#%%

#36. This exercise shows the use of one of NumPy’s sequence functions, which operate on the whole array rather than per element. What is returned by one_dim.sum() ?

import numpy as np
from variable_exercise29_to_36 import one_dim

#The operation result is 15

print(one_dim.sum())
# %%
